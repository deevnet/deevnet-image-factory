# ansible

Ansible playbooks and inventories for image provisioning.

## Directories

- `playbooks/` - Ansible playbooks for image configuration
- `inventories/` - Inventory files for local and remote targets
