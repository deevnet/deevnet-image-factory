# ansible/playbooks

Ansible playbooks for image provisioning.

## Planned Playbooks

- `image-base.yml` - Base image configuration
- `builder-node.yml` - Builder node setup
- `pi-base.yml` - Raspberry Pi base configuration
