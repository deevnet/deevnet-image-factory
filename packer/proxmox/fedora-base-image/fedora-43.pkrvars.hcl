# Fedora Server 43 (previous - retained so the prior template stays buildable)
# NOTE: the artifacts role only publishes the DVD ISO for the *current* release,
# so iso_download_pve is not usable here; this build expects local:iso/.
fedora_release = "43"
fedora_build   = "1.6"
iso_sha256     = "aca06983bef83da9b43144c1a2ff4c8483e4745167c17f53725c16a16742e643"
