# packer/common

Shared Packer variables and configuration files used across all image builds.

## Files

- `variables.pkrvars.hcl` - Common variables (automation user, SSH key URLs, artifact server)
